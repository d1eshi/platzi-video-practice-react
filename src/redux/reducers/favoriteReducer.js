export const favoriteReducer = (state = {}, action) => {
  console.log(state)
  // if (action.type === '@favorites/add_favorite') {
  //   return {
  //     ...state,
  //     mylist: [...state.mylist, action.payload]
  //   }
  // }
  return state
}
